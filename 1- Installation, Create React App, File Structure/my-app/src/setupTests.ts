// jest-dom adds custom jest matchers for asserting on DOM nodes.
// allows you to do things like:
// expect(element).toHaveTextContent(/react/i)
// learn more: https://github.com/testing-library/jest-dom
import '@testing-library/jest-dom/extend-expect';


/**
 * Don't worry too much about this file yet, this is setting up unit tests. This is a section
 * we will ideally get to later if we have time. But this comes packaged together with the rest of the create-react-app features.
 */